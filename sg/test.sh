ip=$(curl -s https://ipinfo.io/ip | tr -d '[:space:]')
echo "Runner IP is $ip"
sleep 10
ip=$(curl -s https://ipinfo.io/ip | tr -d '[:space:]')
echo "Runner IP is $ip"
sleep 10
ip=$(curl -s https://ipinfo.io/ip | tr -d '[:space:]')
echo "Runner IP is $ip"
sleep10
ip=$(curl -s https://ipinfo.io/ip | tr -d '[:space:]')
echo "Runner IP is $ip"
